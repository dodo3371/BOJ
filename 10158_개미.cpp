#include <iostream>
#include <cstdio>
#include <vector>
#include <algorithm>
#include <cstring>
#include <string>
using namespace std;

int main()
{
#ifdef _CONSOLE
	freopen("input.txt", "r", stdin);
#endif

	int nFlag = -1;
	int w, h, p, q, t;

	scanf("%d", &w);
	scanf("%d", &h);
	scanf("%d", &p);
	scanf("%d", &q);
	scanf("%d", &t);

	
}